# Core — usado nos 4 notebooks principais e nos 5 bônus
anthropic>=0.40.0
langgraph>=0.2.0
pydantic>=2.5.0

# LoRA fine-tuning real (Projeto #4, Semana 13.4)
torch>=2.0.0
transformers>=4.40.0
peft>=0.10.0
numpy>=1.26.0

# Rodar os notebooks
jupyter>=1.0.0
ipython>=8.18.0

# Dev (usado pelo Makefile: make test / lint / format)
pytest>=7.4.0
black>=23.12.0
flake8>=6.1.0

# Utilitário
python-dotenv>=1.0.0
